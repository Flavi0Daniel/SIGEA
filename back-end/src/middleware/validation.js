function validateRegister(req, res, next) {
  // Validação simples
  next();
}

function validateLogin(req, res, next) {
  // Validação simples
  next();
}

module.exports = {
  validateRegister,
  validateLogin
};
